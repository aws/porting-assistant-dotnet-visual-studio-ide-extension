name: "Lint PR"

on:
  pull_request_target:
    types:
      - opened
      - edited
      - synchronize

jobs:
  main:
    name: semantic-pull-request
    uses: amannn/action-semantic-pull-request@v4.5.0
